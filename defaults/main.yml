---

# 'local_users' must be made available as variable via e.g. the group_vars
#local_users: {}
#  podman:
#    fullname: podman system user
#    system: true                 # Optional
#    lingering: true              # Optional
#    podman: true                 # Optional
#    uid: 900
#    subordinate_ids:             # Optional
#      uid: 65142784
#      gid: 65142784
#
#  root:
#    fullname: root
#    password: "{{ root_user_password }}"
#    authorized_keys:
#      - "{{ public_key1 }}"
#      - "{{ public_key2 }}"
#    ssh_keys:
#      id_rsa:
#        public_key: "{{ root_user_public_key }}"
#        private_key: "{{ root_user_private_key }}"

# The salt to use for idempotent user password generation.
# NOTE: The length of this salt is only allowed to be 16 characters long.
local_users_password_salt: "{{ inventory_hostname | regex_replace('-', '') | regex_replace('_', '') | truncate(16, True, '') }}"

# Local groups to be created
local_groups: []
#  - group1
#  - group2
